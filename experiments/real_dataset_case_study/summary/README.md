# Generated summaries

Run `python experiments/real_dataset_case_study/summarize.py --output-root <dir>`
after all six audits finish, where `<dir>` is wherever your raw run outputs
actually live (e.g. `experiments/real_dataset_case_study/results/`). Commit
the generated CSV/JSON summaries here; keep the raw dumps themselves
(gitignored, `results*/`) local-only.
