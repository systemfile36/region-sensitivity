---
name: Bug report
about: Something in SSAT doesn't work as documented
title: ""
labels: bug
---

**What happened**

What you expected to happen, and what actually happened instead.

**Minimal reproduction**

A minimal config/command that reproduces the problem, if applicable. Trim it down as far as you can — a config with one region and one perturbation is easier to debug than the full audit that first surfaced the issue.

```yaml
# minimal config here
```

```bash
# command(s) that reproduce it
```

**Environment**

- SSAT version / commit: 
- Python version: 
- OS: 
- CPU or GPU (and GPU model, if relevant): 
- Installed via Docker Compose workspace or a plain `pip install`?

**Logs / traceback**

Paste the relevant `ssat` output, including any `advisory`/`ERROR` lines. Redact file paths or data you don't want public.
